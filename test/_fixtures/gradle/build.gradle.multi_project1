allprojects {
  configurations {
    compile
  }

  repositories {
    maven {
      url 'file://test/_fixtures/gradle/maven/repo1.maven.org/maven2'
    }
  }
}

dependencies {
  compile('backport-util-concurrent:backport-util-concurrent:2.1')
}
