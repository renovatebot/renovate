Versioning scheme for stable Rust toolchain releases.

### Version Format

This currently supports:

- `stable` channel
- `<major.minor>` range (e.g., `1.75`)
- `<major.minor.patch>` version (e.g., `1.75.0`)
