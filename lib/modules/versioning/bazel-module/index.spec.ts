// import bzlmod from '.';

describe('modules/versioning/bazel-module/index', () => {
  describe('getMajor(version)', () => {
    test.todo('implement me');
    // test.each`
    //   version         | execpted
    //   ${'1.2.3'}      | ${1}
    //   ${'20230414.2'} | ${20230414}
    // `('.getMajor("$version")', ({ version, expected }) => {
    //   expect(bzlmod.getMajor(version)).toEqual(expected)
    // });
  });
});
