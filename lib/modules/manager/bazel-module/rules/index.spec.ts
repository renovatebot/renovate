// import { extractDepsFromFragment } from '.';

describe('modules/manager/bazel-module/rules/index', () => {
  it.todo('IMPLEMENT ME!');
  // it.each`
  //   a                                                     | exp
  //   ${'bazel_dep(name = "rules_foo", version = "1.2.3")'} | ${[{ rule: 'bazel_dep', name: 'rules_foo', version: '1.2.3' }]}
  // `('extractDepsFromFragmentData($a)', ({ a, exp }) => {
  //   const fragments = parse(a, 'MODULE.bazel');
  //   if (!fragments) {
  //     throw new Error('Expected fragments not to be null.');
  //   }
  //   expect(fragments).toHaveLength(1);
  //   expect(extractDepsFromFragment(fragments[0])).toEqual(exp);
  // });
});
