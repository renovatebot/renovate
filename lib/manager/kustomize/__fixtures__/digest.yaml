apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hasura

commonLabels:
  app.kubernetes.io/name: hasura

bases:
  - ../base/

patchesStrategicMerge:
  - patches/deployment.yaml

images:
  - name: postgres
    # this is informational, newTag is ignored if digest is set:
    # https://github.com/kubernetes-sigs/kustomize/blob/f61b075d3bd670b7bcd5d58ce13e88a6f25977f2/api/types/image.go#L19
    newTag: 11
    digest: sha256:b0cfe264cb1143c7c660ddfd5c482464997d62d6bc9f97f8fdf3deefce881a8c
