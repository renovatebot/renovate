machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  node:
    version: 6.9

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
    - yarn --version
  override:
    - yarn install --frozen-lockfile
  cache_directories:
    - ~/.cache/yarn

test:
  pre:
    - npm run update-docs
  override:
    - git diff-index --quiet HEAD -- || (echo "There are uncommitted changes" && git status && exit 1)
    - yarn test
  post:
    - bash <(curl -s https://codecov.io/bash)
