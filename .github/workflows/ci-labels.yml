name: ci-labels

on:
  pull_request:
    types:
      - labeled
      - unlabeled

jobs:
  handle-ci-label:
    if: github.event.label.name == 'ci:fulltest'
    uses: ./.github/workflows/build.yml
    permissions:
      contents: write
      issues: write
      pull-requests: write

