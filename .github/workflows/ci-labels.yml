name: ci-labels

on:
  pull_request:
    types:
      - labeled
      - unlabeled

permissions:
  contents: read

jobs:
  handle-ci-label:
    if: github.event.label.name == 'ci:fulltest'
    uses: ./.github/workflows/build.yml

