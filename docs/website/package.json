{
  "name": "renovate-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "docusaurus": "docusaurus",
    "start": "run-s compile && docusaurus start",
    "build": "run-s compile && docusaurus build",
    "compile": "run-s clean generate",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "serve": "docusaurus serve",
    "clean": " git clean -fX -- ./docs/ ./static/",
    "generate": "run-s generate:*",
    "generate:files": "node ../../tools/generate-docs.js"
  },
  "dependencies": {
    "@docusaurus/core": "2.0.0-alpha.70",
    "@docusaurus/preset-classic": "2.0.0-alpha.70",
    "@mdx-js/react": "1.6.22",
    "clsx": "1.1.1",
    "fs-extra": "9.0.1",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "shelljs": "0.8.4"
  },
  "devDependencies": {
    "npm-run-all": "4.1.5"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
