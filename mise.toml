[tools]
node = "24.16.0"
pdm  = "2.27.0"
pnpm = "10.33.4"

# needed for pipx backends e.g. pdm
pipx   = "1.13.0"
python = "3.14.5"

[hooks]
postinstall = '''
pnpm install
pdm install
'''

[settings]
# needed for hooks
experimental = true
